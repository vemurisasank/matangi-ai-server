version: "3.9"

services:

  matangi-server:

    build: .

    container_name: matangi-ai-server

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    volumes:
      - ./projects:/app/projects
      - ./outputs:/app/outputs
      - ./workflows:/app/workflows